- **fix(ci):** clear five Fast Quality HARD failures on `release/v3.8.51`: break the `models.ts` / `providers.ts` / `deletion.ts` import cycle by moving `getProviderConnectionsCount` to a leaf module; list covering unit tests in `stryker.conf.json` `tap.testFiles`; allow `@omniroute/opencode-plugin-v2/` in the npm pack artifact; fill `ProviderCandidate` fixtures so open-sse typecheck stays at baseline 0; replace a `sk-` loopback fixture key that tripped gitleaks ([#12732](https://github.com/diegosouzapw/OmniRoute/issues/12732))
