- **fix(ci):** document six live env vars (`OMNIROUTE_WAL_PASSIVE_INTERVAL_MS`, `OMNIROUTE_WAL_GUARD_MAX_MB`, `OMNIROUTE_VACUUM_MIN_DELETED_ROWS`, `OMNIROUTE_PRESSURE_SELF_RESTART`, `OMNIROUTE_PRESSURE_SELF_RESTART_AFTER_MS`, `OMNIROUTE_SQLJS_WASM_PATH`) in `.env.example` and regenerate `skills/cli-tunnel` for `tunnel create [type]`, clearing two Docs/Merge-integrity HARD failures on `release/v3.8.51` ([#12732](https://github.com/diegosouzapw/OmniRoute/issues/12732))
