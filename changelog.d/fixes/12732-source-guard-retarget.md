- **fix(ci):** retarget source-guard tests to live extract sites; keep Antigravity 422 `error.code` through the provider pipeline; skip quota-aware expansion when a pinned connection row is missing (do not fetch quota for unknown, disabled, or banned pins); split the Cursor CLI version pin out of `node:fs` so the provider-detail client stays browser-safe; drop unused `providers.ts` imports left after the count leaf extract ([#12732](https://github.com/diegosouzapw/OmniRoute/issues/12732))
