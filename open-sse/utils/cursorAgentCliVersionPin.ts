/**
 * Pinned Cursor Agent CLI build id. No Node builtins so dashboard
 * client bundles can import the pin without pulling node:fs.
 * Bump when refreshing Cursor CLI impersonation.
 */
export const CURSOR_AGENT_CLI_VERSION = "2026.07.08-0c04a8a";
